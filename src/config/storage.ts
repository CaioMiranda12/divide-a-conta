export const storageConfig = {
  folderName: "divide-a-conta/bills",
  maxImageWidthInPixels: 1600,
  maxImageSizeInBytes: 1024 * 1024 * 10,
  allowedImageMimeTypes: ["image/jpeg", "image/png", "image/webp"] as const,
} as const;

